# CTRG Grant System - Replit Development Environment
SECRET_KEY=django-insecure-replit-dev-key-replace-in-production-abcdef123456
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0,.replit.dev,.repl.co,.replit.app

# PostgreSQL (Replit built-in)
DATABASE_ENGINE=django.db.backends.postgresql
DATABASE_NAME=heliumdb
DATABASE_USER=postgres
DATABASE_PASSWORD=password
DATABASE_HOST=helium
DATABASE_PORT=5432

# Email (console backend for development)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=localhost
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=CTRG Grant System <noreply@nsu.edu>

# Frontend URL (for invitation links, emails, etc.)
FRONTEND_URL=http://localhost:5000

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:5000,http://127.0.0.1:5000,http://localhost:5173,http://0.0.0.0:5000
CORS_ALLOW_CREDENTIALS=True

# Security (relaxed for development)
SECURE_SSL_REDIRECT=False
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False
SECURE_HSTS_SECONDS=0
SECURE_HSTS_INCLUDE_SUBDOMAINS=False

# File Upload
FILE_UPLOAD_MAX_MEMORY_SIZE=52428800
DATA_UPLOAD_MAX_MEMORY_SIZE=52428800

# Encryption (disabled for local dev)
FILE_ENCRYPTION_KEY=

# Application
TIME_ZONE=UTC
LANGUAGE_CODE=en-us
